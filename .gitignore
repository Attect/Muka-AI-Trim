# Build output
/target
**/*.pdb

# Machine-specific: this pins target-dir to a drive letter, which would break
# someone else's clone. Keep it local.
/.cargo/config.toml

# Scratch from running the binary in the tree
*.log
# 误留在工作树里的构建目录（WSL 把 target-dir 当成相对路径时会生成 E:/...）
/E*/
